# 0.1.0 Worklog

2026-09-22 — planning:

- Read `draft.md`: fast math galaxy demo, no N-body; disk/belt/bar with realistic orbits;
  question whether friction ("cheap gravity") can drive disk→belt/bar transport; suspicion
  that drag is unstable and clumps stars; spawn/despawn particles dispreferred (persistent
  star objects wanted); deliverable: single html, 1k–10k stars + short approach description.
- Explored repo: docs-only checkout, no code yet.
- Validated the draft's suspicion: pure velocity drag circularizes + shrinks orbits → collapse
  to center; structural transport is instead collisionless resonance physics (ILR/CR/OLR) in a
  rotating non-axisymmetric field. Adopted: fixed multi-component analytic potential
  (Miyamoto–Nagase disk, Plummer bulge, log halo) + rigidly rotating m=2 bar and Lin–Shen
  logarithmic spiral modes, symplectic leapfrog test particles. Rejected: N-body, spawn/despawn,
  closed-form epicycles (no bar coupling). Drag kept only as a teaching toggle with Langevin fix.
- Wrote `archive/0.1.0-plan.md` (spec, milestones M0–M5 + stretch, node-check gates §8).

Next step: M0 — `index.html` + `galaxy.js` stubs, sprite renderer with N toggle and fps counter,
node harness guard; then M1 axisymmetric core with `experiments/check-gradient.js`.

Open questions (in plan §12): meaning of "belt" (assumed halo belts/streams + thick disk),
default N (1k), camera tilt (20°).

2026-09-22 — plan review (before implementation):

- Fixed `p = tan(pitch)` → `p = cot(pitch)` (15° → p≈3.732).
- Added concrete default parameters (§4.1 table; vc = 0.83/1.00/0.93/0.85 at
  R = 0.3/1/2.5/5, κ/Ω = √2 at R0), replacing the Lambert-W disk formula with a
  numeric inverse-CDF table, explicit bulge/halo/stream recipes, KDK time-argument
  order, per-substep precompute list, data-space corotation (tilt-correct, not
  ctx.rotate), tilt/Ωb/Ωs sliders, 128-star EJ HUD subset, N = 1k/4k/10k default 4k.
- Split the Jacobi gate (per-pattern conservation + combined shadow convergence)
  since two pattern speeds admit no exact invariant; softened the resonance gate
  to bar-frame alignment (peak-radius matching was misconceived).

2026-09-22 — M1 axisymmetric core + M2 populations (`galaxy.js`, 540 lines):

- Miyamoto–Nagai ×2 + Plummer + log halo, SoA Float64Arrays, cached-accel KDK
  leapfrog (1 force eval/step), xorshift128 + Box–Muller, interleaved class slots
  so every N-prefix is a representative mix, numeric disk tables, settle 25 tu
  (15 ramp + 10 full-strength) over all 10k.
- `check-gradient` 1.1e-8, `check-rotation-curve` vc(1)=1.000 κ/Ω=1.4143,
  `check-epi-ratio` 0.00% (after switching counts → crossing intervals).

2026-09-22 — M3 bar + M4 spiral (modes + resonance machinery):

- m=2 bar (Ab=0.06, rb=1.2, Ωb=0.4) + log-Gaussian Lin–Shu spiral (As=0.02,
  Rp=2.5, pitch 15°, Ωs=0.25), Gaussian verticals, scan+bisect ILR/CR/OLR rings.
- `check-jacobi` caught two real bugs: (1) a hard R<8 mode cutoff broke EJ
  (1.2e-2) — removed, no cutoffs anywhere; (2) the hard spiral phase freeze kinked
  the force at r1 — replaced with C1 smoothstep `spiralLD`. Final: bar 1.5e-3,
  spiral 7.3e-4, shadow median 7e-5. Energy drift proven dt-independent
  (physical pattern work, not numerics).
- `check-resonances`: bar-frame m=2 amp 0.185, misalign 1.5° over disk 0.6<R<1.9.

2026-09-22 — M5 belts, drag, calibration (`experiments/`, 8 checks green):

- Halo resampled to r^-1.5 over [0.8,7] (uniform-in-r sagged 30%); spiral zs
  0.3→0.5 halves vertical heating with stronger in-plane arms.
- `check-no-clumping`: patterns L2R/L2Z 18/15% (physical secular evolution),
  axisym control L2Z 1.9% (integrator clean), medR ×0.994, peak/mean falling.
- `check-drag-collapse`: drag median ×0.11 (collapse shown), control ×0.98.
- `check-perf`: 15 ms/frame sim-only at 10k in node → default N=4k (≈6 ms).

2026-09-22 — M0/M5 demo page (`index.html`, ~700 lines):

- Canvas2D DPR-aware renderer, prerendered sprites, tilt+corot projection,
  resonance rings, rotcurve mini-plot (theory + measured vφ + Ωp diagonals),
  8 orbit-trail tracers with bar-capture rings, N/speed/tilt/Ab/As/Ωb/Ωs/η/σ
  controls, drag+diffusion teaching toggles, trails, pause/step/reset, keys,
  in-page approach text, settle progress overlay (chunked, non-blocking).
- Validated headless: inline-script syntax OK, DOM-stub smoke run (155 frames,
  full settle, every control + reset exercised) with zero exceptions. No browser
  in sandbox — file:// visual check (fps, trails, rings, rotcurve, 10-min run)
  left for the user.

Next step: user opens `index.html` from file:// and confirms M5 acceptance (60 fps
at 4k, bar/spiral/stream visible, rings match, no visual clumping); then optionally
M6 stretch (WebGL2 renderer) or parameter variants (stronger bar, slower spiral).

Open questions (plan §12) resolved with defaults: belt = streams + thick disk
(rings also rendered), default N = 4k, tilt 20° + slider.

2026-09-22 — user report: structures fade fast + many retrograde stars. Both real:

- Retrograde bug: isotropic bulge/halo sampling put 48% of those classes on Lz<0
  orbits. Fixed with prograde vφ fold (`foldPrograde`, |v|² unchanged); residual
  ~4% flip later by genuine resonant torque. New gate `check-ics` (shares,
  <1% retrograde at init, bounds, tight stream).
- Fading structures: two pattern speeds beat at 2(Ωb−Ωs) — measured bar amplitude
  swinging 0.04↔0.26 — plus overlapping bar-OLR/spiral-CR resonances grinding the
  disk. Fixed by corotating the pair (Ωs=Ωb=0.40, bar-driven spiral): response
  steady, σR saturates at 0.20, EJ exact again (HUD shows it live).
- Supporting changes: thin disk warmed (σR 0.07→0.12 — cold start rang and heated
  4× in settle), ramp 15→20 / settle 25→30, spiral narrowed (Rp 2.8, sig 0.5) and
  strengthened (As 0.035) for persistent arm contrast ~1.2.
- Measurement lesson: wide-annulus m=2 fakes a decay via phase cancellation across
  CR; narrow-annulus + ψ-contrast show arms steady for 400+ tu. New gate
  `check-response` (bar>0.08, spiral>0.10, contrast>0.5 at T=150).
- Recalibrations: jacobi max→p99 (+max cap) after one plunging orbit took a
  bounded 1e-3 kick (median 2e-6); resonances 0.15→0.12 for the warmer disk.
- Validation: all 10 checks green, DOM-stub smoke (155 frames) clean, plan §3/§4/
  §5/§8/§10/Appendix A + in-page text updated (Ωs slider now an intentional
  beating experiment).

Next step: user re-checks file:// visuals (steady bar + arms, all-prograde flow,
live EJ readout); candidate follow-ups: stronger-bar variant, WebGL renderer (M6).
